# build output (one dir per target)
build/

# secrets (copy from secrets.h.example)
secrets.h

# ESP-IDF per-project generated config
sdkconfig
sdkconfig.old
**/sdkconfig
**/sdkconfig.old

# merged flash image
firmware.bin

# ESP-IDF managed components (re-fetched from the registry on build;
# dependencies.lock is committed to pin versions)
**/managed_components/

# editor / OS
*.swp
.DS_Store
